apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: provisioning-vault
  namespace: cluster
spec:
  # Set this once to the Vault endpoint reachable from the ACM Hub.
  address: "https://CHANGE_ME_VAULT_ADDRESS:8200"
  skipTLSVerify: true
